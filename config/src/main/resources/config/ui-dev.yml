spring:
  rabbitmq:
          host: localhost
          port: 5672
          username: guest
          password: guest
  redis:
    host: localhost
    port: 6379
  thymeleaf:
    cache: false
    check-template-location: true
    enabled: true
    encoding: utf-8
    mode: HTML5
    content-type: text/html
    prefix: classpath:/templates/
    suffix: .html
